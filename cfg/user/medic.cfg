exec user/resetbinds

bind	"T"			"say_team == UBER READY ==;vm_negative"
bind	"MOUSE2"	"+attack2;say_team == UBER USED! == UBER USED! == UBER USED! == UBER USED! == UBER USED! == UBER USED! ==;vm_jeers;-attack2"
bind	"B"			"say_team == UBER FAKED ==;vm_uberready"
